/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Optional rewrites, uncomment if needed:
  // async rewrites() {
  //   return [
  //     { source: "/auth", destination: "/auth" },
  //     { source: "/logs", destination: "/logs" },
  //   ];
  // },
};

module.exports = nextConfig;
